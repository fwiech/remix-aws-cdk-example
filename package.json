{
  "private": true,
  "name": "@remix-aws-cdk-example/root",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "yarn --cwd remix postinstall",
    "start": "yarn --cwd remix start",
    "build": "yarn --cwd remix build",
    "synth": "yarn --cwd infrastructure synth",
    "deploy": "yarn --cwd infrastructure deploy"
  },
  "workspaces": ["infrastructure", "remix"],
  "devDependencies": {
    "typescript": "^4.5.4"
  }
}
